# Note ý tưởng mở rộng của thầy:

- Có thời gian thì tìm hiểu thêm scylladb
- Giao diện: thầy gợi ý dùng Stitch để nó gen ra ảnh giao diện rồi ném lên antigravity để nó code, hoặc export sang figma (hình như bây giờ nó ko cho nữa) để sửa tiếp (vì stitch chỉ gen ảnh chứ ko sửa dc), hoặc sang google ai studio để code demo ứng dụng đơn giản
- có thể đồng bộ google ai studio với github
- google ai studio phù hợp để làm demo đơn giản thôi, mặc định thường làm FE bằng react.js